import styled from 'styled-components'

export const Main = styled.main`
    position: relative;
    min-height: 100vh;
    padding-top: 2rem;
    padding-bottom: 2rem;
    display: flex;
    flex-direction: column;

    section {
        margin-bottom: 2rem;
    }

    h1 {
        line-height: 4rem;
        font-size: 2.5rem;
    }
`
