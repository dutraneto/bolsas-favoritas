import React from 'react'

import * as S from './styled'

import Header from '../Header'

const Layout = (props) => {
    // Hooks here

    return (
        <S.LayoutWrapper>
            <Header />
        </S.LayoutWrapper>
    )
}

export default Layout
